<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @insurer.name %>
</p>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

<tbody>
<% @messages.each do |m| %>
<tr>
        <td><%= m.authnum %></td>
        <td><%= m.sender_id %></td>
        <td><%= link_to 'approve', controller: "messages", action:"approve", id: m.id %>
        <td><%= link_to 'deny', controller: "messages", action:"deny", id: m.id %>

    <% end %>
  </tbody>
</table>
<%= link_to 'Edit', edit_insurer_path(@insurer) %> |
<%= link_to 'Back', insurers_path %>
